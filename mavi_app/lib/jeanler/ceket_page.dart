import 'package:flutter/material.dart';

class CeketPage extends StatelessWidget {
  const CeketPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}