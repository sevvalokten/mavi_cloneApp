import 'package:flutter/material.dart';

class SeninfitPage extends StatelessWidget {
  const SeninfitPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}