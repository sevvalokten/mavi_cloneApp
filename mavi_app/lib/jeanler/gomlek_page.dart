import 'package:flutter/material.dart';

class GomlekPage extends StatelessWidget {
  const GomlekPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}